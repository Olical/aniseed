version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run: apt-get update
      - run: apt-get install make neovim
      - run: make deps
      - run: make test
