version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run: apt-get update
      - run: apt-get --assume-yes install make git neovim luajit
      - run: make deps
      - run: make test
